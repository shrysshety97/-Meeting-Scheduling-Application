# ── MongoDB ──────────────────────────────────────────────────────────────────
MONGODB_URI=mongodb://localhost:27017/meeting-scheduler

# ── Server ───────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── URLs ─────────────────────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000

# ── Email — Resend (REAL emails) ─────────────────────────────────────────────
# 1. Sign up free at https://resend.com
# 2. Go to API Keys → Create API Key
# 3. Paste it below
RESEND_API_KEY=re_gR9dgkro_CmkPFdHjhf7cg2bWqfiN7TrQ

# From address — options:
#   a) Use Resend's free test address: onboarding@resend.dev  (no domain needed)
#   b) Use your own domain after verifying at resend.com/domains
RESEND_FROM_EMAIL=Climatiq Meetings <onboarding@resend.dev>

# ── Availability config (optional overrides) ─────────────────────────────────
# AVAILABILITY_START=16
# AVAILABILITY_START_MIN=30
# AVAILABILITY_END=18
# AVAILABILITY_END_MIN=0
# SLOT_DURATION_MINUTES=15
